apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fem.fullname" . }}-test-connection"
  labels:
    {{- include "fem.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fem.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
