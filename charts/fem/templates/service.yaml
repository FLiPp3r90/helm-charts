apiVersion: v1
kind: Service
metadata:
  name: {{ include "fem.fullname" . }}
  labels:
    {{- include "fem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fem.selectorLabels" . | nindent 4 }}
